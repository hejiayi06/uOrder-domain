import { SortBySortPipe } from './sort-by-sort.pipe';

describe('SortBySortPipe', () => {
  it('create an instance', () => {
    const pipe = new SortBySortPipe();
    expect(pipe).toBeTruthy();
  });
});
